# Legacy Next.js Frontend

This package is deprecated and is not part of the official Ernest deployment path.

Use `frontend-svelte/` for the published dashboard. The Next.js package remains in the repository only as a temporary migration reference and can be built manually with:

```bash
pnpm run frontend-next:build
```
